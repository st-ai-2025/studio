
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow users to read and write to the 'chats' collection
    // if they are authenticated.
    match /chats/{chatId} {
      allow read, write: if request.auth != null;
    }
  }
}
